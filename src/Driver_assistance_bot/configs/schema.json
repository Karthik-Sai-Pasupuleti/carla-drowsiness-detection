{
    "title": "Driver_Drowsiness_Assessment",
    "description": "Structured reasoning for driver drowsiness risk assessment.",
    "type": "object",
    "properties": {
        "drowsiness_level": {
            "type": "string",
            "description": "Overall drowsiness risk level.",
            "enum": [
                "low",
                "medium",
                "high",
                "critical"
            ]
        },
        "reasoning": {
            "type": "string",
            "description": "Brief explanation based on input metrics that led to the decision."
        }
    },
    "required": [
        "drowsiness_level",
        "reasoning"
    ]
}
